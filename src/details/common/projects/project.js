import React from 'react';
import './project.scss';

export class Project extends React.Component {
    static defaultProps = {
        className: 'project'
    };

    render() {
        const {value, className} = this.props;

    }
}